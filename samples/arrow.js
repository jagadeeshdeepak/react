const fn = (name) => {
  console.log(name);
}

// incase u need to return from this function
const fn1 = (name) => {
  return "Hi " + name;
}

// the above can simply be written as
const fn2 = (name) => "Hi" + name;
